{
  "name": "mjez29.github.io",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "react": "^16.0.0",
    "react-bootstrap": "^0.31.5",
    "react-dom": "^16.0.0",
    "react-scripts": "1.0.17"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject",
    "deploy": "git push -d origin master && npm run build && git subtree push --prefix build origin master"
  },
  "homepage": "https://mjez29.github.io",
  "devDependencies": {
    "gh-pages": "^1.0.0"
  }
}
